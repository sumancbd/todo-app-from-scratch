# A Stripe TEST secret key — it starts sk_test_. Without it the app still
# runs; the upgrade button just says payments are not set up.
STRIPE_SECRET_KEY=

# How many todos the free plan allows before it refuses (default 5).
FREE_LIMIT=5

PORT=3000
